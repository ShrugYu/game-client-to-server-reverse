// app 模块 build.gradle（片段）
plugins { id 'com.android.application' }
android {
    namespace 'com.rev.redirect'
    compileSdk 34
    defaultConfig { minSdk 24 targetSdk 34 }
}
dependencies {
    // Xposed API 只编译期用，不打进包
    compileOnly 'de.robv.android.xposed:api:82'
}
